{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 0 },
      "id": 1,
      "targets": [{ "expr": "wroclaw_sky_aircraft", "legendFormat": "aircraft" }],
      "title": "Aircraft in snapshot",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "short", "max": 1, "min": 0 } },
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 0 },
      "id": 2,
      "targets": [{ "expr": "wroclaw_sky_live", "legendFormat": "live" }],
      "title": "Live poller active",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "short", "max": 1, "min": 0 } },
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 0 },
      "id": 3,
      "targets": [{ "expr": "wroclaw_sky_circuit_open", "legendFormat": "circuit_open" }],
      "title": "OpenSky circuit open",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": { "defaults": { "unit": "short" } },
      "gridPos": { "h": 4, "w": 6, "x": 18, "y": 0 },
      "id": 4,
      "targets": [{ "expr": "wroclaw_sky_sse_clients", "legendFormat": "sse" }],
      "title": "SSE clients",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 4 },
      "id": 5,
      "targets": [
        { "expr": "rate(wroclaw_sky_refresh_total[5m])", "legendFormat": "refresh/s" },
        { "expr": "rate(wroclaw_sky_refresh_errors_total[5m])", "legendFormat": "errors/s" }
      ],
      "title": "Refresh rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 4 },
      "id": 6,
      "targets": [
        { "expr": "rate(wroclaw_sky_alerts_total[5m])", "legendFormat": "alerts/s" },
        { "expr": "rate(wroclaw_sky_webhook_errors_total[5m])", "legendFormat": "webhook errors/s" }
      ],
      "title": "Alerts & webhooks",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["wroclaw-sky", "adsb"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "browser",
  "title": "wroclaw-sky",
  "uid": "wroclaw-sky",
  "version": 1
}
